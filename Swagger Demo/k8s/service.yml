apiVersion: v1
kind: Service
metadata:
  name: swagger-demo-service

spec:
  type: LoadBalancer
  selector:
    app: swagger-demo

  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30007